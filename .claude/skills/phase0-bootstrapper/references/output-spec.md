# Output specification

The canonical artifact shapes, required sections, skeletons, and the
managed-marker merge rule live in **`docs/output-schema.md`** (this repo) and
its portable copy at **`skills/phase0-bootstrapper/resources/output-schema.md`**.

Read those files — this pointer is kept here so existing links do not break.
